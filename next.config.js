/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable App Router features
  experimental: {
    // Remove if not needed, but keeping for compatibility
  },
}

module.exports = nextConfig
